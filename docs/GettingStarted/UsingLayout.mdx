import { Meta } from "@storybook/addon-docs";

<Meta title="Getting Started/Using Layout"/>

# The Layout component

The sharp-eyed readers among you may have noticed the `Layout` component.

What's it for?

Under the hood, the Layout component exports a simple `<g>` tag.  The `<g>` lets you group multiple SVGs together!
